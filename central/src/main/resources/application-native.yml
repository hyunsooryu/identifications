spring:
  application:
    name: config-server

  profiles:
    active: native

  cloud:
    config:
      server:
        native:
          search-locations: file:///Users/hyunsoo/workplace/identifications/central/config-repo


server:
  port: 9281
