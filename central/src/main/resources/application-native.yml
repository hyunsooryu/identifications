spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        native:
          search-locations: file:///Users/hyunsoo/workplace/identifications/central/config-repo
        bootstrap: false

server:
  port: 9281
