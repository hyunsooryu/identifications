spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://localhost:9281
      profile: dev
      name: gateway

  config:
    import: optional:configserver:http://localhost:9281